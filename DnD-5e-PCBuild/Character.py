"""
To create;
    1. Create/Set character in P{dict} format,
        a. to then export to json / build in compatibility
    2. Database with DnD values (races, classes, monsters, items)
        a. to then use as keys in json character sheet
    3. Visualise DnD character sheet with graphics, using either json or variable dict as input
        In order of priority;
        a. Function (tkinter)
        b. Graphics (nice to have)